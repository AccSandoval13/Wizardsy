const React = require('react')
const Def = require('./default')

function gallery () {
    return (
        <div>
            {
            //    connect content here through jsx
            }
        </div>
    )
}